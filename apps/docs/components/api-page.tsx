'use client';

import { createOpenAPIPage } from 'fumadocs-openapi/ui';

/** One REST endpoint's reference: parameters, responses, code samples and a request playground. */
export const OpenAPIPage = createOpenAPIPage();
